{
  "name": "weather-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass": "node-sass -w scss -o dist/css/ --recursive"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  },
  "dependencies": {
    "axios": "^1.2.4",
    "node-sass": "^8.0.0"
  }
}
